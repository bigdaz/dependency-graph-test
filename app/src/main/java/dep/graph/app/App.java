/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package dep.graph.app;

import dep.graph.list.LinkedList;

import static dep.graph.utilities.StringUtils.join;
import static dep.graph.utilities.StringUtils.split;
import static dep.graph.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
